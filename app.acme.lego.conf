# -------------------------------------------------------------------------------------------------------------------- #
# COMMON
# -------------------------------------------------------------------------------------------------------------------- #

# CA hostname (and optionally :port).
# The server certificate must be trusted in order to avoid further modifications to the client.
# If you’d like to test something without issuing real certificates, consider using the staging endpoint instead:
# https://acme-staging-v02.api.letsencrypt.org/directory
ACME_SERVER='https://acme-staging-v02.api.letsencrypt.org/directory'
# ACME_SERVER='https://acme-v02.api.letsencrypt.org/directory'

# Email used for registration and recovery contact.
ACME_EMAIL='mail@fdn.com'

# Directory to use for storing the certificate and certificate key.
ACME_CERT_PATH='/etc/ssl/acme'

# Set the webroot folder to use for HTTP-01 based challenges to write directly to the '.well-known/acme-challenge' file.
# This disables the built-in server and expects the given directory to be publicly served with access to
# '.well-known/acme-challenge'.
ACME_WEB_ROOT='/var/www/html'

# The password used to encrypt the .pfx (PCKS#12) file.
ACME_PFX_PASSWORD='pa$$word'

# The encoding format to use when encrypting the .pfx (PCKS#12) file.
# Supported: RC2, DES, SHA256.
ACME_PFX_FORMAT='RC2'

# Key type to use for private keys.
# Supported: rsa2048, rsa3072, rsa4096, rsa8192, ec256, ec384.
ACME_KEY_TYPE='ec256'

# -------------------------------------------------------------------------------------------------------------------- #
# SERVICES
# -------------------------------------------------------------------------------------------------------------------- #

ACME_SERVICES=(
  'angie.service'
  'nginx.service'
  'apache2.service'
  'postfix@-.service'
  'prosody.service'
)
